SCREEN 12: WIDTH 80, 25: KEY OFF: CLS : COLOR 6
LOCATE 25, 30: PRINT "PRESS SPACE BAR"
FOR I = 0 TO 255
IF I = 11 THEN PRINT I, "CURSOR HOME": GOTO 80
IF I = 12 THEN PRINT I, "CLS & HOME": GOTO 80
IF I MOD 16 = 0 THEN A$ = INPUT$(1)
PRINT I, CHR$(I)
80 NEXT
LOCATE 25, 30: PRINT SPC(9);
 

